// Create a function that takes in two arguments and returns a concatenated string in the console.

function attach(argument1, argument2){
	var together = argument1 + " and " + argument2;
	return together;
}


console.log(attach("hi","hello"));